const header = document.getElementById('header');

setInterval(() => {
    header.classList.toggle('hidden'); // Toggle the 'hidden' class
}, 500); // Adjust the time (in milliseconds) for faster or slower flashing

